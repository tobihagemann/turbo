# Resolve PR Comments: Standard Path

Hand off to `/turboplan` with the accepted findings as the task description. Turboplan handles drafting, refinement, user confirmation, implementation, and finalize internally.

## Task Tracking

Use `TaskCreate` to create a task for each phase:

1. Run `/turboplan` skill

## Phase 1: Run `/turboplan` Skill

Run the `/turboplan` skill with the accepted findings (including any items reclassified in SKILL.md Step 6) as the task description. The commit SHA from finalize is needed for reply messages.

Check your task list for remaining tasks and proceed.
